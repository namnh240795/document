export declare function verifyHmacSignature(body: string | Buffer, signature: string, secret: string): boolean;
export declare function generateHmacSignature(body: string | Buffer, secret: string): string;
